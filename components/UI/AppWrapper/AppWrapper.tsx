import styled from '@emotion/styled';

const AppWrapper = styled.div`
  background: url('/assets/img/app-background.png') center / cover no-repeat;
  width: 100vw;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default AppWrapper;
